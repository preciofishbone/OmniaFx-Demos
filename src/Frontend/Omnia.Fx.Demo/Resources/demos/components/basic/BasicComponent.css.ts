import { style } from "typestyle";

/** convert a style object to a CSS class name */
export const redStyle = style({ color: 'red', });
export const blueStyle = style({ color: 'green', fontSize: 22 });