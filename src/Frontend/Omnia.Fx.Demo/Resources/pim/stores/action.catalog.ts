import * as SessionActions from "./session/PimSession.actions";
import * as TaskActions from "./tasks/PimTasks.actions";

export const ActionCatalog =
{
    Session : SessionActions,
    Tasks: TaskActions
}
